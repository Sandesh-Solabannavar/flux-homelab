apiVersion: v1
kind: Service
metadata:
  name: traefik-dashboard
  namespace: traefik
spec:
  type: LoadBalancer
  LoadBalancerIP: 172.18.0.81
  ports:
    - name: web
      port: 80
      targetPort: 80
  selector:
    app: traefik
