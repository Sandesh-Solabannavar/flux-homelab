apiVersion: v1
kind: ConfigMap
metadata:
  name: homepage-config
  namespace: homepage
data:
  custom.js: ""
  custom.css: ""
  settings.yaml: |
    title: My Homepage
    theme: dark
    layout:
      - Monitoring

  services.yaml: |

  bookmarks.yaml: |

  widgets.yaml: |

  docker.yaml: |
    # Optional
