apiVersion: apps/v1
kind: Deployment
metadata:
  name: data-scraper-us
  namespace: data-scraper
  labels:
    app: data-scraper-us
spec:
  replicas: 1
  selector:
    matchLabels:
      app: data-scraper-us
  template:
    metadata:
      labels:
        app: data-scraper-us
    spec:
      containers:
        - name: data-scraper-us
          image: 192.168.1.11:5000/scraper:latest
          imagePullPolicy: IfNotPresent # Important for using remote images
          ports:
            - containerPort: 3000
          env:
            - name: COUNTRY
              value: US
            - name: METHOD
              value: main
            - name: ORDER
              value: asc
            - name: LIMIT
              value: "100"
          envFrom:
            - secretRef:
                name: data-scraper-secrets
          resources:
            requests:
              cpu: "500m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
