apiVersion: apps/v1
kind: Deployment
metadata:
  name: data-scraper-jp
  namespace: data-scraper
  labels:
    app: data-scraper-jp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: data-scraper-jp
  template:
    metadata:
      labels:
        app: data-scraper-jp
    spec:
      containers:
        - name: data-scraper-jp
          image: ghcr.io/sandesh-solabannavar/zoominfo-scraper:8a499e7
          imagePullPolicy: IfNotPresent # Important for using remote images
          ports:
            - containerPort: 3000
          env:
            - name: COUNTRY
              value: JP
            - name: METHOD
              value: main
            - name: ORDER
              value: asc
            - name: LIMIT
              value: "100"
          envFrom:
            - secretRef:
                name: data-scraper-secrets
          resources:
            requests:
              cpu: "500m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
      imagePullSecrets:
        - name: ghcr-login-secret
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: data-scraper-jp-desc
  namespace: data-scraper
  labels:
    app: data-scraper-jp-desc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: data-scraper-jp-desc
  template:
    metadata:
      labels:
        app: data-scraper-jp-desc
    spec:
      containers:
        - name: data-scraper-jp-desc
          image: ghcr.io/sandesh-solabannavar/zoominfo-scraper:8a499e7
          imagePullPolicy: IfNotPresent # Important for using remote images
          ports:
            - containerPort: 3000
          env:
            - name: COUNTRY
              value: JP
            - name: METHOD
              value: main
            - name: ORDER
              value: desc
            - name: LIMIT
              value: "100"
          envFrom:
            - secretRef:
                name: data-scraper-secrets
          resources:
            requests:
              cpu: "500m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
      imagePullSecrets:
        - name: ghcr-login-secret
---
